on:
  push:
    branches:
      - main

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - run: git config -l
      - run: cat ~/.gitconfig
      - uses: actions/checkout@v3
      - run: cat .git/config
